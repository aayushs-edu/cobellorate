document.addEventListener('DOMContentLoaded', () => {
    const submit_b = document.getElementById('submit_b');

    submit_b.addEventListener('click', () => {
        window.href("dashboard_page.html");
    });
});